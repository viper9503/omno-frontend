import { SettingsPage } from '@/features/settings/components';

export default function InviteMembersScreen() {
  return (
    <SettingsPage title="Invite Members" backTitle="Team Settings">
      {null}
    </SettingsPage>
  );
}
